<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout android:id="@+id/content_main"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:showIn="@layout/activity_main">

    <android.support.design.widget.TextInputLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:padding="16dp">

        <EditText
            android:id="@+id/email_edit_text"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="@string/email_hint"
            android:imeOptions="actionDone"
            android:inputType="textEmailAddress"
            android:maxLines="1"/>


    </android.support.design.widget.TextInputLayout>

    <pl.droidsononroids.coolloginbutton.view.LoginButton
        android:background="?android:attr/selectableItemBackground"
        android:id="@+id/loginButton"
        android:layout_height="56dp"
        android:layout_width="match_parent"
        android:layout_gravity="bottom"
        app:text="@string/app_name"
        app:textsize="@dimen/signuptextsize"
        app:success_color="@color/colorSuccess"
        app:failure_color="@color/colorError"
        app:default_color="@color/colorDefaultLoginButton"
        app:failure_icon="@drawable/ic_warning_white_24dp"
        app:success_icon="@drawable/ic_check_white_24dp"/>

    <!--Miejsce na twój button (wysokość = 56dp)-->

</android.support.design.widget.CoordinatorLayout>
